# Pinned Rust toolchain for ODP_PatinaSmbiosDemo (UEFI targets).
#
# SPDX-License-Identifier: MIT
#

[toolchain]
channel = "nightly-2025-12-12"
targets = ["x86_64-unknown-uefi", "aarch64-unknown-uefi"]
components = ["rust-src", "clippy", "rustfmt"]
