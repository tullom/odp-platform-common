# patina_tianocore - local ignore rules.
#
# SPDX-License-Identifier: MIT
#

# Libraries don't commit Cargo.lock — downstream binaries pick their own
# resolved versions. This local ignore prevents accidental commits when
# `cargo` regenerates the lockfile during local development.
Cargo.lock
